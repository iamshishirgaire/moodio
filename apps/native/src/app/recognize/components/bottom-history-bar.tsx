import { StyleSheet, View } from "react-native";

const BottomHistoryBar = () => <View style={styles.container} />;

export default BottomHistoryBar;

const styles = StyleSheet.create({
	container: {
		width: "100%",
		backgroundColor: "#272525ff",
		alignItems: "center",
	},
});
